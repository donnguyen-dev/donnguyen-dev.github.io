# Oscar Foxtrot Pty Ltd

Public company website. This repository contains only website content, not the Picklist Doctor application source.

Build: `npm ci` then `npm run build`. GitHub Pages serves the validated static export from `docs/`.
